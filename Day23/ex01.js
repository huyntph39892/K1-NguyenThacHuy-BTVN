/**
 *  *Co ? cach de gan su kien?
 * 1 gan su kien bang attribute html
 * 2 dung js de gan
 * 3 dung eventListener
 */

const btnElement = document.getElementById("btn")
btnElement.onclick = function() {
    console.log(event)
}